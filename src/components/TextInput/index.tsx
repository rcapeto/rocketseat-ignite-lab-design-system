import { Input, type TextInputProps } from './Input';
import { Root, type TextInputRootProps } from './Root';
import { Icon, type TextInputIconProps } from './Icon';

export const TextInput = { 
   Root: Root,
   Input: Input,
   Icon: Icon,
};

export { 
   TextInputProps,
   TextInputRootProps,
   TextInputIconProps
};